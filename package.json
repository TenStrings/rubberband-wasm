{
  "name": "rubberband-wasm",
  "version": "1.0.0",
  "description": "Standalone static-page pitch-shifting demo using Rubber Band compiled to WebAssembly.",
  "main": "main.js",
  "scripts": {
    "prepare:ffmpeg": "node scripts/prepare-ffmpeg-assets.mjs",
    "predev": "npm run prepare:ffmpeg",
    "prebuild": "npm run prepare:ffmpeg",
    "dev": "npm run build:wasm && vite",
    "build:web": "vite build",
    "build:wasm": "./build.sh --wasm-only",
    "build": "./build.sh",
    "preview": "vite preview --port 8080",
    "test": "echo \"No tests configured\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TenStrings/rubberband-wasm.git"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-2.0-or-later",
  "type": "module",
  "bugs": {
    "url": "https://github.com/TenStrings/rubberband-wasm/issues"
  },
  "homepage": "https://github.com/TenStrings/rubberband-wasm#readme",
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  }
}
